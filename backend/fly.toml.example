# fly.toml app configuration file generated for laravel-nuxt-template-backend
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#
# 使用方法：
# 1. このファイルをコピーして fly.toml を作成
# 2. app名を任意のユニークな名前に変更
# 3. 必要に応じてregionやその他の設定を調整

app = 'your-app-name-backend'  # ← ユニークなアプリ名に変更してください
primary_region = 'nrt'          # ← 必要に応じて地域を変更（nrt=東京）
kill_signal = 'SIGINT'
kill_timeout = '5s'

[experimental]
  auto_rollback = true

[build]
  dockerfile = 'Dockerfile.fly'

[env]
  APP_ENV = 'production'
  LOG_CHANNEL = 'stderr'
  LOG_LEVEL = 'info'
  LOG_STDERR_FORMATTER = 'Monolog\Formatter\JsonFormatter'

[[services]]
  protocol = 'tcp'
  internal_port = 8080
  processes = ['app']

  [[services.ports]]
    port = 80
    handlers = ['http']
    force_https = true

  [[services.ports]]
    port = 443
    handlers = ['tls', 'http']

  [services.concurrency]
    type = 'connections'
    hard_limit = 25
    soft_limit = 20

  [[services.tcp_checks]]
    interval = '15s'
    timeout = '2s'
    grace_period = '1s'